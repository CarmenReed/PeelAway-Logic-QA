# AI Skills Inventory

> **Purpose:** Track every AI tool, technique, and skill demonstrated in building PeelAway Logic.
> **Why:** This inventory maps directly to interview talking points and demonstrates breadth + depth of AI experience.
> **Auto-update:** Claude Code should update this file when new AI capabilities are added to the project.

---

## AI Development Tools

| Tool | How Used in PeelAway | Proficiency |
|------|---------------------|-------------|
| **Claude Code (Anthropic)** | Primary development partner for all React code, tests, CI/CD, architecture docs | Production daily use |
| **Claude Cowork (Anthropic)** | Planning, auditing, recursive iteration on architecture and interview prep | Production daily use |
| **Anthropic Claude API** | Runtime LLM for job scoring (Haiku) and resume tailoring (Sonnet) | Production integration |
| **Claude Web Search Tool** | ATS board job discovery (Greenhouse, Lever, Workday) via web_search_20250305 | Production integration |
| **Google Gemini Gems** | Original job search tools (resume reviewer, job scorer, cover letter writer) before migration | Retired, documented in ADR-004 |
| **Microsoft Semantic Kernel** | Python orchestration demo mirroring PeelAway pipeline phases | Demo/portfolio |
| **Azure AI Search** | Full-text + filtered search over indexed scored jobs | Portfolio integration |

## AI Architecture Patterns

| Pattern | Where Applied | Evidence |
|---------|--------------|----------|
| **Human-gated pipeline** | All 5 phases require explicit user approval before API calls | ADR-003, ReviewPhase.jsx |
| **Anti-hallucination prompt engineering** | TAILOR_SYSTEM constant constrains output to resume-derivable content only | ADR-006, prompts.js |
| **Multi-model orchestration** | Haiku for batch scoring (cost), Sonnet for tailoring (quality) | constants.js, api.js |
| **Agentic pipeline architecture** | 5-phase Scout > Search > Review > Tailor > Complete with state management | JobSearchPipeline.jsx |
| **RAG-adjacent retrieval** | Resume text extraction (PDF.js) feeds profile data into scoring prompts | profileExtractor.js, ScoutPhase.jsx |
| **Batch processing with rate limiting** | 8-job batches with 15s delays to avoid API throttling | constants.js (SCORING_BATCH_SIZE, SCORING_BATCH_DELAY_MS) |
| **Confidence-based tiering** | LLM scores mapped to Strong/Possible/Weak tiers with human override | utils.js, ReviewPhase.jsx |
| **Provider-agnostic design** | Azure OpenAI swap-ready via Semantic Kernel; REST wrapper abstracts provider | ADR-001, api.js |

## AI-Assisted Development Practices

| Practice | How Applied |
|----------|------------|
| **AI pair programming** | Claude Code writes initial implementations; Carmen reviews, tests, iterates |
| **AI-driven testing** | 346 tests across 15 files, most generated via Claude Code with human review |
| **AI-assisted documentation** | Architecture docs, ADRs, knowledge base authored collaboratively with AI |
| **Recursive AI auditing** | Using Cowork to audit plans generated by Claude Code; meta-level quality improvement |
| **Prompt iteration** | Scoring and tailoring prompts refined through multiple Claude Code sessions |
| **AI-powered CI/CD** | GitHub Actions workflows generated and maintained via Claude Code |
| **AI-assisted code review** | PR reviews and debugging scenarios documented for AI-assisted workflow |

## Microsoft/Azure AI Skills

| Skill | Demonstrated By |
|-------|----------------|
| **Azure AI Search** | REST integration in azureSearchService.js (index creation, batch indexing, full-text search, filtering) |
| **Semantic Kernel** | Python plugins (JobScoringPlugin, ResumeParserPlugin) orchestrating pipeline via SK kernel |
| **Azure OpenAI (swap-ready)** | AzureChatCompletion commented-in code in SK demo, documented migration path in ADR-001 |
| **Bicep/IaC** | Documentation template covering 7 Azure resources (Search, OpenAI, Static Web Apps, Cosmos DB, ADF, App Insights, Key Vault) |
| **Azure DevOps** | Daily use at SpaceGenius (pipelines, managed SQL instances, Service Bus) |
| **Cosmos DB (architecture)** | Documented as localStorage replacement in Bicep template and ADR-005 |
| **Azure Data Factory (architecture)** | Documented as scheduled job ingestion pipeline in Bicep template |

### Azure AI Search - Technical Detail

| Capability | Implementation |
|-----------|---------------|
| **Index creation** | `createJobIndex(endpoint, key)` sends a PUT request with a typed schema: id (key), title/description (searchable), company/source (filterable), score/datePosted (sortable). Returns `{ success, message }` and never throws. |
| **Batch indexing** | `indexJobs(endpoint, key, jobs)` splits any job array into 50-document chunks and POSTs each batch to the `docs/index` endpoint. Returns `{ indexed, errors }` aggregate result across all batches. |
| **Full-text search** | `searchJobs(endpoint, key, query, filters)` POSTs to `docs/search.post.search` with `search`, `top: 50`, and `orderby: score desc`. Supports OData `$filter` strings built from a plain-object filter argument. |
| **Regression test coverage** | `src/__tests__/azureSearchService.test.js` covers all 4 exported functions (9 tests): happy path, HTTP error, network failure, and batching boundary (120 jobs into 3 batches of 50/50/20). |

### Semantic Kernel - Technical Detail

| Capability | Implementation |
|-----------|---------------|
| **Python kernel setup** | Kernel initialized with `OpenAIChatCompletion` service; `AzureChatCompletion` is a commented-in drop-in replacement requiring only `AZURE_OPENAI_ENDPOINT` and `AZURE_OPENAI_KEY` env vars. |
| **Plugin authoring** | `JobScoringPlugin` and `ResumeParserPlugin` implemented as native Python classes with `@kernel_function` decorators. Each function has typed parameters and return annotations consumed by the SK planner. |
| **Function invocation** | Pipeline phases invoke SK functions directly via `kernel.invoke()`. No planner required for the demo path; the orchestration sequence mirrors the 5-phase PeelAway pipeline. |
| **Multi-phase orchestration** | Scout, Score, Review, Tailor, and Complete phases are each represented as discrete SK function calls chained in sequence, demonstrating how the browser pipeline maps to a Python orchestration layer. |

### Azure OpenAI - Swap-Ready Design Pattern

| Aspect | Detail |
|--------|--------|
| **Current provider** | OpenAI API (via Anthropic API in main app; OpenAI SDK in SK demo) |
| **Swap target** | Azure OpenAI Service with `AzureChatCompletion` |
| **What changes** | One service registration line in the SK kernel; two environment variables (`AZURE_OPENAI_ENDPOINT`, `AZURE_OPENAI_KEY`) |
| **What stays the same** | All plugin functions, orchestration logic, and prompt templates are provider-agnostic |
| **Decision record** | ADR-001 documents the tradeoffs and the migration path |

### Anti-Hallucination - Grounding Constraints

| Technique | Where Applied |
|-----------|--------------|
| **Resume-grounded output** | `TAILOR_SYSTEM` in `src/constants.js` instructs the model to derive all resume content from the uploaded profile only. Fabricated metrics, titles, and certifications are explicitly prohibited. |
| **Prompt-layer enforcement** | Each tailor prompt in `src/prompts.js` reinforces grounding constraints in both the system prompt and user message. No separate post-processing filter is used; the constraint is upstream. |
| **Regression test coverage** | Tailor phase tests in `tailorPhase.test.js` verify that the system prompt constant is passed on every API call. Scoring tests verify `total_score` bounds (0-10) and required field presence to catch hallucinated schema deviations. |
| **Documented rationale** | ADR-006 explains the grounding strategy and why prompt-layer enforcement was chosen over output validation. |

## SpaceGenius Production AI

| Implementation | Description |
|---------------|-------------|
| **Automated onboarding** | AI-assisted onboarding materials for global consultant teams |
| **PR review automation** | Claude-assisted pull request review workflow |
| **Backlog triage** | AI assessment of work items for consultant assignment |
| **Retrospective synthesis** | Read AI meeting notes fed into AI for ceremony prep |
| **Strangler Fig with AI** | Using AI to plan and execute monolith decomposition |
| **Custom plugins/extensions** | SpaceGenius-specific AI tools for existing processes |

---

## Update Log

| Date | Change | Files Affected |
|------|--------|---------------|
| 2026-04-12 | Initial inventory created during Azure integration sprint | All new docs/architecture/ files |
| 2026-04-12 | Azure AI Search + Semantic Kernel integration planned | azureSearchService.js, semantic-kernel-demo/ |
| 2026-04-12 | Added technical detail sections for Azure AI Search, Semantic Kernel, Azure OpenAI swap pattern, and anti-hallucination grounding | docs/AI_SKILLS_INVENTORY.md |
